const button = document.querySelector('.button');

button.addEventListener('click', guardar);

function guardar(){
    alert("Gracias por intentar matricularse, próximamente se habilitará el enlace!");
}